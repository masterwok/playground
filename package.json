{
  "name": "playground",
  "version": "1.0.0",
  "description": "Used as a playground for TypeScript JS development.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others \"npm run build:watch\" \"npm run http\" ",
    "build:watch": "webpack --colors --progress --watch",
    "http": "http-server ./dist -p 1337"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^3.4.0",
    "html-webpack-plugin": "^2.28.0",
    "http-server": "^0.10.0",
    "ts-loader": "^2.1.0",
    "typescript": "^2.3.4",
    "webpack": "^2.6.1"
  }
}
